cmake_minimum_required(VERSION 2.6)
project(splat)
find_package(VTK REQUIRED)
include(${VTK_USE_FILE})
add_executable(splat splat.cxx "../vtkGaussianScalarSplatter.cxx")
target_link_libraries(splat ${VTK_LIBRARIES})
